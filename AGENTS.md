# AGENTS.md

## Cursor Cloud specific instructions

This is a newly initialized repository (`aimemo`). As of the initial setup, the repo contains only a `README.md` with the project title.

- **No dependencies** to install — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar manifest exists.
- **No services** to run — no application code, Docker configurations, or dev server scripts exist.
- **No tests or linting** configured yet.
- **Runtime available on the VM:** Node.js v22, Python 3.12, Git 2.43.

When source code and dependency manifests are added, update the VM environment setup script accordingly (via `SetupVmEnvironment`) and expand this section with startup/run caveats.
